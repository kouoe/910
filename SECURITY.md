# 安全策略 · Security Policy

## 支持的版本

| 版本 | 是否维护 |
| :--- | :--- |
| 1.0.x | ✅ 维护中 |
| < 1.0 | ❌ 不再维护 |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。**

请通过 GitHub 的 [Security Advisories](https://github.com/kouoe/910/security/advisories) 私密提交，或发送至仓库维护者私信。报告中请包含：

- 漏洞类型与影响范围
- 复现步骤与最小验证代码片段
- 潜在危害评估

我们会在 7 个工作日内响应，确认后协同你发布修复版本，并在修复公开时致谢。

## 凭据管理约定

本项目严格遵循以下约定：

- DashScope API Key、数据库账号密码等敏感值**只允许通过环境变量注入**
- `application.yml` 中仅保留 `${VAR}` 占位符，**不得出现任何真实密钥**
- `.env` 等本地配置文件已被 `.gitignore` 忽略
- `.gitignore` 额外忽略了 Docker 数据卷（`docker/volumes/`），防止运行时数据误提交

若你曾在历史提交中误传过密钥，请**立即在对应平台吊销并重新生成**，仅删除文件内容不足以消除风险——Git 历史仍可被检索。

## 已知安全加固措施

| 项目 | 措施 |
| :--- | :--- |
| Druid 监控页 | 已关闭 `stat-view-servlet`（规避历史 CVE 未授权访问） |
| 用户输入 | AI 入参经敏感词黑白名单过滤 |
| 回答生成 | 系统提示词强制约束不得编造商品与价格信息 |
| 依赖更新 | 由 Dependabot 每周自动检查并提 PR |
